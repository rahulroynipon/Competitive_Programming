#include<bits/stdc++.h>
using namespace std;

class Node{
    public:
    Node* prev = nullptr;
    int val = val;
    Node* next = nullptr;
};

void push(Node *&head,int val){
    Node* newNode = new Node(val);
    if(head==nullptr)
}

int main(){

    

    return 0;
}